<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="crud.mybatis_board.board.BoardMapper">
<!-- 게시물 등록 -->
  <insert id="createBoard" parameterType="Board">
    INSERT INTO board (id, title, content, writer)
    VALUES (board_seq.nextval, #{title}, #{content}, #{writer})
  </insert>
  <!-- 게시물 조회 -->
  <select id="getBoard" parameterType="Long" resultType="Board">
    SELECT * FROM board WHERE id = #{id}
  </select>

  <!-- 게시물 수정 -->
  <update id="updateBoard" parameterType="Board">
  UPDATE board SET title = #{title}, content = #{content}, writer = #{writer}, regDate = #{regDate}
  WHERE id = #{id}
    </update>

  <!-- 게시물 삭제 -->
  <delete id="deleteBoard" parameterType="Long">
    DELETE FROM board WHERE id = #{id}
  </delete>

  <!-- 게시글 전부 가져오기 -->
  <select id="getBoardList" resultType="Board">
    SELECT * FROM board ORDER BY id
  </select>
  
</mapper>